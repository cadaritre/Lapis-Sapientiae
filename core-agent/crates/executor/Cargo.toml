[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
actions = { path = "../actions" }
planner = { path = "../planner" }
logging = { path = "../logging" }
