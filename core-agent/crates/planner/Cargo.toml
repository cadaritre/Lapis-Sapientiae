[package]
name = "planner"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
actions = { path = "../actions" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
